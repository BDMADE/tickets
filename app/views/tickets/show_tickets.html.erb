<div class="container">
  <div class="row">
    <div class="col-md-12 col-xs-12">
      <!-- general form elements -->
      <div class="box box-primary">
        <div class="box-body">
          <blockquote class="blockquote">
            <p><b><%= @ticket.subject %></b></p>
            <p><%= @ticket.message %></p>
            <footer class="blockquote-footer"><%= @ticket.user.name %>(<%= @ticket.user.email %>)</footer>
          </blockquote>
        </div>
      </div>
    </div>
  </div>
</div>

<%= render @ticket.ticket_replies %>

<%= render "ticket_replies/form" %>
